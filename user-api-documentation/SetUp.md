## Step-by-Step Setup & Testing

1. Clone the repository.
2. Install required dependencies (e.g., Node.js, Express if backend implemented).
3. Run the API server locally.
4. Use Postman or cURL to test endpoints.
5. Validate inputs using rules in `Validation.md`.
6. Confirm correct responses using examples in `Examples.md`.
